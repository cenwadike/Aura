version: "3.9"

services:
  aura-api:
    build: .
    container_name: aura-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env      
    volumes:
      - ./logs:/app/logs    # persist logs on host
      